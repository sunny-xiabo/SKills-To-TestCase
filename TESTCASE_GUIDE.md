# 测试用例生成指南 · TESTCASE_GUIDE.md

> **定位：** 纯对话工具的操作说明（ChatGPT、Copilot Chat、无脚本环境的对话等）。  
> **有 Cursor / Claude Code 且已 init 时：** 请用 `/testcase-creator`，执行细则以 Skill（`prompt.md` + `references/`）为准；本文件可作话术参考，不必与脚本命令一一对齐。  
> **装机 / 升级 / 仓库结构：** 见仓库根目录 [README.md](README.md)。  
> 使用方式：将本文件全文复制，连同检查点/评审点索引等一起粘贴到对话开头。  
> 版本对应：testcase-creator **1.12.0**（见 CHANGELOG）。

---

## 使用前准备

在开始对话前，请将以下内容一并复制粘贴给 AI：

1. 本文件全文
2. `.testcase-assets/project.config.md` 全文
3. `.testcase-assets/checkpoints-index.md` 全文
4. `.testcase-assets/review-expectations-index.md` 全文
5. **增量模式时**：再粘贴基线 `2-用例定稿.md`（或明确说明从哪次 history 复制）

粘贴格式建议：

```
=== 测试用例生成指南 ===
[粘贴本文件内容]

=== 项目配置 ===
[粘贴 project.config.md 内容]

=== 检查点索引 ===
[粘贴 checkpoints-index.md 内容]

=== 评审点索引 ===
[粘贴 review-expectations-index.md 内容]

=== 基线定稿（仅增量模式）===
[粘贴 2-用例定稿.md]

=== 需求或变更内容 ===
[粘贴完整需求，或仅本次变更说明]
```

> **需求与设计来源类型**：支持以下 **9** 种，可选择一种或多种，请在对话开头说明使用了哪种：
> - **A** 直接粘贴文字需求描述
> - **B** 乐享页面链接
> - **C** 接口文档链接或本地文件路径
> - **D** 本地技术方案文件（.md / .docx / .pdf）
> - **E** 图片/截图（.png / .jpg / .jpeg / .gif / .webp）— UI 设计稿、原型图、流程图
> - **F** 飞书文档链接（feishu.cn / larksuite.com）
> - **G** Excel 需求列表（.xlsx / .xls）
> - **H** 需求管理工具链接（Jira / Tapd / 禅道）
> - **I** 代码（粘贴关键文件内容，或说明模块路径；须限定范围，禁止「全仓」空泛描述）
>
> 设计稿请先导出为 PDF（选择 D）或图片（选择 E），可与其他需求来源组合提供。纯对话工具无法自动读本地路径时，请粘贴代码片段或上传导出文件。代码仅用于反推测试点，**不生成自动化测试代码**。推荐用法：优先 diff/小路径；有验收请同时用 A；确认前可改正文分析；勿扫公共 utils；可多轮收窄补扫。

---

## 核心原则

- 先确认输入，不自动导入，不生成自动化测试代码
- 每个阶段结束后等待用户确认，再进入下一阶段
- 检查点和评审点可随项目经验持续沉淀
- 生成用例的**备注列必须留空**；过程信息写在准备文档 / 变更分析 / 评审报告
- 优先级默认：P0=异常，P1=正向主流程/边界，P2=并发，P3=体验类

---

## 运行模式

对话开始后，先确认模式（用户也可直接说「全量」或「增量」）：

```
【运行模式】
A. 全量新建（完整五阶段）
B. 增量变更（基于历史定稿做补/改/废）
```

| 模式 | 适用 | 主路径 |
|------|------|--------|
| A 全量 | 新模块、第一次写用例 | 阶段 1→2→3→4→5 |
| B 增量 | 需求只改了一截 | 基线 + 变更 → 分析 → 变更集 → 合并全表 → 4 或 5 |

---

## 模式 B：增量变更

### B1 确认基线

用户粘贴基线 `2-用例定稿.md`，AI 统计条数与最大用例编号，用于续号。

### B2 本次变更

只收集**本次变更**（可组合 A–H 与设计稿），不要要求用户重贴完整 PRD。

### B3 影响分析（确认门）

```
[变更影响分析 - 请确认]

- 基线：…（N 条）
- 变更摘要：…
- 影响模块/对象：…
- 规则变化：新增 / 修改 / 删除 / 未变

建议动作预估：
| 动作 | 数量（估） | 说明 |
|------|------------|------|
| 新增 | n | … |
| 修改 | n | 对应基线 ID |
| 废弃 | n | 对应基线 ID，原因 |
| 保持 | n | 仅计数 |

回复「确认」后生成变更集；指出错误则修正分析。
```

### B4 变更集与合并

**编号规则**：新增从最大主号 +1 连续分配；修改保持原 ID；废弃 ID 不复用、**不进入有效表**（只在变更摘要中列出）。

输出：

1. `1-变更集.md` 结构：新增表 / 修改表（完整新行）/ 废弃表  
2. 合并后的完整有效用例表（= 基线 − 废弃 + 修改替换 + 新增）  
3. 询问：进入评审（阶段 4）或跳过评审直接定稿（阶段 5）

定稿文首须含「本轮变更摘要」（新增/修改/废弃 ID）。备注列仍留空。

> [!NOTE]
> 手动保存：`0-变更分析.md`、`1-变更集.md`、`1-评审记要.md`、`2-用例定稿.md` 到  
> `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/`

---

## 模式 A：五阶段执行流程

### 阶段 1：需求与设计 / 代码输入

收到对话后（参考上述 9 种来源类型），分别提取需求、设计与代码信息。若同时提供需求和设计稿/代码，保留来源归属并交叉检查；不得用设计或代码覆盖未确认的文字需求。未提供设计稿/代码时省略对应部分。

```
[需求与设计解析结果 - 请确认]

- 测试对象：[功能/模块名称]
- 业务规则：
  1. [规则1]
  2. [规则2]
- 限制条件：[如：仅限已登录用户]
- 接口/端点：[如有]
- 涉及角色：[如：普通用户、管理员]

输入完备性：需求 / 设计 / 代码 各自 OK·降级·失败·未提供

设计稿分析：
- 设计范围：[文件/页面/画板/节点名称]
- 页面与流程：[入口、页面流转、关键操作路径]
- 组件与字段：[表单项、按钮、列表、弹窗等]
- 交互与状态：[默认/加载/空/禁用/错误/成功等状态]
- 视觉与适配：[层级、间距、颜色、字号、响应式/多端差异]
- 校验与反馈：[输入约束、错误文案、操作反馈]

需求与设计差异：
| 类型 | 需求描述 | 设计表现 | 影响/待确认项 |
|------|----------|------------|---------------|
| 缺失/冲突/补充 | ... | ... | ... |

若解析正确，请回复「确认」继续阶段 2。
若需修正，请指出错误内容。
```

---

### 阶段 2：检查点推荐与（可选）历史复用

#### 2a 检查点选择（含推荐）

展示已粘贴的检查点索引，按分类列出，并根据需求/设计/业务域给出**推荐预勾**（排除 `[已废弃]`；推荐不宜过多，例如合计不超过约 15 条）。

```
[阶段 2a - 检查点选择]
已根据需求预推荐（可改）：
  [*] UC-01 ...
  [ ] FILE-01 ...（未推荐，可手选）

请输入：编号列表 / 采用推荐 / 全选 / 跳过
```

不得静默替用户选定。快捷语「采用推荐」仅在用户明确回复后生效。

#### 2b 历史用例复用（可选）

若用户粘贴了其它历史定稿或摘要，可列出候选供勾选；也可回复「跳过」。

```
[阶段 2b - 历史用例复用]（可跳过）
  [1] TC-012  提交绩效  正向  P1
  [2] TC-015  重复提交  异常  P0
请输入要复用的编号，或「跳过」
```

**未经用户勾选，不得复制历史用例。** 增量模式不做 2b（基线已是历史源）。

收到选择后，生成结构化摘要：

```
[输入结构化结果 - 0-用例准备]

需求要素：
| 项目 | 内容 |
|------|------|
| 测试对象 | ... |
| 业务规则 | ... |
| 限制条件 | ... |

设计要素（如提供设计稿）：
| 项目 | 内容 |
|------|------|
| 页面与流程 | ... |
| 组件与字段 | ... |
| 交互与状态 | ... |
| 视觉与适配 | ... |
| 校验与反馈 | ... |
| 待确认差异 | ... |

已关联检查点：
| 编号 | 描述 | 分类 | 来源 |
|------|------|------|------|
| UC-01 | 手机号格式校验 | 用户中心 | 推荐 |

历史复用映射（如有）：
| 本轮用例ID（阶段 3 填写） | 历史用例ID | 测试点 |
|--------------------------|------------|--------|
| （待分配） | TC-012 | ... |

结构化完成，请回复「生成用例」进入阶段 3。
```

> [!NOTE]
> 手动将此内容保存为 `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/0-用例准备.md`

---

### 阶段 3：用例生成

用户回复「生成用例」后，基于需求要素 + 设计稿要素（如有）+ 检查点输出完整用例表。覆盖设计中明确呈现的页面流程、组件字段、交互状态、视觉适配与校验反馈；差异项在确认前不得擅自选定需求或设计为准。

若 2b 有勾选复用：

- 本轮**统一重新编号**（如从 TC-001 起），在准备文档回填映射  
- 只为未覆盖的规则/检查点/场景补新行  
- 映射不写进备注列  

```
[测试用例表 - 初稿]

| 用例ID | 所属模块 | 测试点 | 前置条件 | 操作步骤 | 预期结果 | 关联检查点 | 场景类型 | 优先级 |
|--------|----------|--------|----------|----------|----------|------------|----------|--------|
| TC-001 | 用户中心 | ...    | ...      | 1. ...   | ...      | UC-01      | 正向     | P1     |
| TC-002 | 用户中心 | ...    | ...      | 1. ...   | ...      | UC-01      | 异常     | P0     |
| TC-003 | 用户中心 | ...    | ...      | 1. ...   | ...      | RISK-01    | 边界     | P1     |
| TC-004 | 用户中心 | ...    | ...      | 1. ...   | ...      | RISK-01    | 并发     | P2     |

共生成 X 条：正向 X / 异常 X / 边界 X / 并发 X
其中复用历史 X 条，新编写 X 条（如有）

请回复「进入评审」进入阶段 4，或告诉我需要修改的内容。
```

> [!NOTE]
> 手动将此内容保存为 `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/1-评审记要.md`  
> 备注列若存在必须为空。

---

### 阶段 4：评审优化（分维度 + 多轮）

> 纯对话工具无法启动隔离 subagent，采用「角色扮演」模式按维度**串行**评审：  
> 按 UX/DATA/COMP/EXEC/BUG/SEC/PERF 等逐维度审查，每个维度完成后输出该维度报告，最后合并。  
> 第 2 轮起启用增量评审，仅审查新增/修改用例。

用户回复「进入评审」后，展示评审点索引，让用户选择评审维度。

**评审范围**：

- 第 1 轮：全量用例参与评审  
- 第 2 轮及之后：已有用例仅展示 ID+测试点摘要，新增/修改用例展示完整信息  

**逐维度审查**（按分类逐个执行）：

```
请以独立测试评审专家的身份，专注于「{维度名称}」维度，对以下用例表进行审查。

## 需求要素
[粘贴需求摘要]

## 设计稿要素（如有）
[粘贴设计摘要与已确认的差异处理结论]

## 评审维度：{维度名称}
[仅粘贴该维度的评审点编号和描述]

## 待评审用例表
[粘贴用例表 — 第 1 轮为全量，后续轮次为增量]

请输出：
1. 评审报告表格（评审点 / 是否已覆盖 / 分析说明 / 建议补充用例）
2. 建议补充用例表（如有，需包含完整的用例行信息）
3. {维度名称} 维度评审结论
```

**合并结论**：

```
[分维度评审合并结论]

| 维度 | 评审点 | 评审结论 | 处理方式 |
|------|--------|----------|----------|
| UX   | UX-01  | [OK]     | 已覆盖 |
| BUG  | BUG-01 | [FAIL]   | 未覆盖，补充 TC-005 |
| COMP | COMP-02| [WARN]   | 需关注 |

[建议补充用例]（按维度去重）
| 用例ID | 测试点 | 来源维度 |
|--------|--------|----------|
| TC-005 | ...    | BUG |
| TC-006 | ...    | COMP |

请选择操作：
  A. 接受所有补充（合并后继续评审）
  B. 仅接受共识项（忽略待确认项）
  C. 部分接受（指定编号）
  D. 手动修改
  E. 忽略，进入阶段 5
```

**文件输出**：

- 评审报告写入 `1-评审报告-第N轮.md`（N 为当前轮次）  
- `1-评审记要.md` 仅保留最终用例表（不含评审记录）  

支持多轮迭代，直到用户满意后进入阶段 5。

---

### 阶段 5：定稿导出

输出完整最终用例表（增量须含变更摘要），询问导出平台（可多选）：

```
[阶段 5 - 定稿导出]
用例定稿已完成，共 X 条。

请选择导出平台（可多选，逗号分隔）：
  J. Jira CSV（.csv，可直接导入 Jira）
  E. Excel（.xlsx 表格，带颜色分类、冻结表头）
  X. XMind（思维导图，用于用例展示和评审）
  N. 不需要导出，本次到此结束

[流程结束摘要]
| 项目 | 详情 |
|------|------|
| 模式 | 全量 / 增量 |
| 测试对象 | ... |
| 用例总数 | X 条（正向X / 异常X / 边界X / 并发X） |
| 关联检查点 | X 个 |
| 评审轮次 | X 轮 |
| 运行目录 | .testcase-assets/history/<运行目录>/ |

是否有新的检查点或评审点需要沉淀？回复「沉淀」继续。
```

#### 纯对话工具下的导出说明

- **主交付物是 Markdown 定稿表**，请先保存为 `2-用例定稿.md`。  
- **不要让 AI 手写整份 `export_data.json`**（易出错）。在有 Python 的机器上用脚本从 MD 生成。  
- Jira CSV 可由 AI 按表生成，或使用 `md_to_csv.py`。  
- 优先级映射：P0→High，P1→Medium，P2/P3→Low；未写优先级时并发按 **P2/Low**。

本地脚本（在项目根、已 init 的环境）：

```bash
# 质检
python3 .testcase-assets/scripts/testcase_quality.py \
  <运行目录>/2-用例定稿.md \
  --audit-output <运行目录>/audit-summary.md --strict

# Jira CSV
python3 .testcase-assets/scripts/md_to_csv.py \
  <运行目录>/2-用例定稿.md \
  <运行目录>/jira_export.csv

# Excel / XMind：先 MD→JSON，再导出
python3 .testcase-assets/scripts/md_to_json.py \
  <运行目录>/2-用例定稿.md \
  <运行目录>/export_data.json \
  --project "<项目名>" --module "<模块名>"

python3 .testcase-assets/scripts/export_excel.py \
  <运行目录>/export_data.json <运行目录>/testcases.xlsx

python3 .testcase-assets/scripts/export_xmind.py \
  <运行目录>/export_data.json <运行目录>/testcases.xmind
```

> [!NOTE]
> 手动将定稿保存为 `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/2-用例定稿.md`

---

### 资产沉淀（任意阶段可触发）

用户输入「沉淀」后，收集新检查点/评审点信息，输出追加内容：

```
[新增检查点]
- [XX-XX] 描述内容  <!-- 新增于 YYYY-MM-DD，来源：[评审问题/用户补充/导入问题/漏测风险] -->

[新增评审点]
- [XX-XX] 描述内容  <!-- 新增于 YYYY-MM-DD，来源：... -->
```

也可从缺陷列表提炼后追加。编号只增不改；废弃项标注 `[已废弃]`，不删除。

> [!NOTE]
> 手动将上述内容追加到对应的 `.testcase-assets/checkpoints-index.md` 或  
> `.testcase-assets/review-expectations-index.md` 文件末尾对应分类下。

---

## 快速触发话术

直接复制以下内容，粘贴到对话开头（附上资产内容后）：

### 完整流程（全量）

```
请按照「测试用例生成指南」走全量模式（A），帮我生成测试用例。
来源类型：[A/B/C/D/E/F/G/H/I，可多选，如 A,D 或 I 或 A,I]
需求内容如下：

[粘贴需求]

设计稿导出文件（如有）：

[填写本地 PDF 路径，或上传 PDF/PNG/截图]
```

### 增量变更

```
请按照「测试用例生成指南」走增量模式（B）。
基线定稿如下：

[粘贴 2-用例定稿.md]

本次变更如下：

[粘贴变更说明 / 设计稿差异]
```

### 仅评审已有用例

```
请按照阶段 4 评审流程，对以下用例表进行分维度评审（串行即可）。
请选择评审维度（如：UX,DATA,COMP,EXEC,BUG）。

[粘贴用例表]
```

### 仅导出定稿用例

```
请按阶段 5 给出导出指引。我已有定稿 Markdown，将在本机执行脚本：
质检 → md_to_csv / md_to_json → export_excel / export_xmind。
请根据下表核对字段是否齐全，并给出应使用的命令（填好路径占位）。

项目名称：[填写项目名]
模块名称：[填写模块名]

[粘贴定稿用例表]
```

### 追问补充用例

```
请基于现有用例，针对以下检查点补充更多场景用例：
- [XX-XX] [检查点描述]

当前用例表：

[粘贴用例表]
```

### 资产沉淀

```
请按照资产沉淀流程，帮我新增以下内容：

新增检查点：
- [域标识] [描述]

新增评审点：
- [维度标识] [描述]
```

### 快速生成单模块用例

```
请帮我针对 [模块名] 生成测试用例，重点关注以下场景：
1. [场景1]
2. [场景2]
3. [场景3]

无需走完整流程，直接生成用例表即可。备注列留空。
```

---

*对应 testcase-creator 1.12.0 · 最后更新：2026-08-14*
